# Pinned loosely; see pyproject.toml for the authoritative dependency list.
# Install the package itself with:  pip install -e .
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
torch>=2.0
openpyxl>=3.1
